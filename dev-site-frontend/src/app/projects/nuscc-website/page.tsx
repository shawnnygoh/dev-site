import ProjectPage from '@/components/ProjectPage';

export const metadata = {
  title: 'NUSCC Website',
  description: 'Redeveloping the frontend for the official NUSCC website.'
};

export default function NUSCCWebsitePage() {
  return <ProjectPage slug="nuscc-website" />;
}