import ProjectPage from '@/components/ProjectPage';

export const metadata = {
  title: 'ByteSize',
  description: 'A web app that generates bite-sized videos from text documents like PDFs that are easily digestible.'
};

export default function ByteSizePage() {
  return <ProjectPage slug="bytesize" />;
}